version: '3'
services:

  db:
    restart: always
    image: postgres:alpine
    volumes:
    - /var/data/docker-data/catgram-postgres:/var/lib/postgresql/data

  redis:
    restart: always
    image: redis:alpine
    volumes:
    - /var/data/docker-data/catgram-redis:/data

  web:
    build: .
    image: us.gcr.io/centi-gitlab/catgram
    restart: always
    env_file: .env.production
    command: /mastodon/web_boot.sh
    ports:
      - "3000:3000"
    depends_on:
      - db
      - redis

  streaming:
    build: .
    image: us.gcr.io/centi-gitlab/catgram
    restart: always
    env_file: .env.production
    command: npm run start
    ports:
      - "4000:4000"
    depends_on:
      - db
      - redis

  sidekiq:
    build: .
    image: us.gcr.io/centi-gitlab/catgram
    restart: always
    env_file: .env.production
    command: bundle exec sidekiq -q default -q mailers -q pull -q push
    depends_on:
      - db
      - redis
