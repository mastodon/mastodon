- content_for :page_title do
  = t('custom_emojis.new.title')

= simple_form_for @custom_emoji, url: settings_custom_emojis_path do |f|
  = render 'shared/error_messages', object: @custom_emoji

  .fields-group
    = f.input :shortcode, placeholder: t('custom_emojis.shortcode'), hint: t('custom_emojis.shortcode_hint')
    = f.input :image, input_html: { accept: 'image/png' }, hint: t('custom_emojis.image_hint')

  .actions
    = f.button :button, t('custom_emojis.upload'), type: :submit
