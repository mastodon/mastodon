.redirect
  .redirect__logo
    = link_to render_logo, root_path

  .redirect__message
    %h1= t('redirects.title', instance: site_hostname)
    %p= t('redirects.prompt')
    %p= link_to @redirect_path, ensure_valid_protocol!(@redirect_path), rel: 'noreferrer noopener'
