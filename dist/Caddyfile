(instance1_backend) {
        reverse_proxy 192.168.x.x:3000 {
                lb_policy least_conn
                fail_duration 30s
                max_fails 3
                header_up Host {host}
                header_up X-Real-IP {remote_host}
                header_up Proxy ""
                transport http {
                        read_timeout 3600s
                        response_header_timeout 3600s
                }
        }
}

(instance1_backend_streaming) {
        reverse_proxy 192.168.x.x:4000{
                lb_policy least_conn
                fail_duration 30s
                max_fails 3
                header_up Host {host}
                header_up X-Real-IP {remote_host}
                header_up Proxy ""
        }
}

aus.social {
        encode {
                zstd
                gzip 6
                minimum_length 1024
        }

        header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload"
        header ?X-Content-Type-Options nosniff

        @static_assets path /assets/* /avatars/* /emoji/* /headers/* /ocr/* /packs/* /shortcuts/* /sounds/*
        handle @static_assets {
                header Cache-Control "public, max-age=2419200, must-revalidate"
                import instance1_backend
        }

        handle /sw.js {
                header Cache-Control "public, max-age=604800, must-revalidate"
                import instance1_backend
        }

        handle /system/* {
                header Cache-Control "public, max-age=2419200, immutable"
                header Content-Security-Policy "default-src 'none'; form-action 'none'"
                import instance1_backend
        }

        handle /api/v1/streaming* {
                import instance1_backend_streaming
        }
        handle {
                import instance1_backend
        }

        }handle_errors {
                rewrite * /500.html
                root * /home/mastodon/live/public
                file_server
        }
}
