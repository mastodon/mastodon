# In test, compile the NodeJS code as if we are in production
NODE_ENV=production
# Federation
LOCAL_DOMAIN=cb6e6126.ngrok.io
LOCAL_HTTPS=true

# Remove once vite integration is desired
VITE_RUBY_SKIP_ASSETS_PRECOMPILE_EXTENSION=true
